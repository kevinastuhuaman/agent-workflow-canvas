# Approval Is A State, Not A Modal

An irreversible action cannot rely on a transient confirmation dialog. The workflow enters a durable `needs review` state with the evidence, changed fields, policy reason, and proposed action visible together.

Approval belongs to one run and one committed state. Editing workflow inputs, changing permissions, or starting another run invalidates it.
