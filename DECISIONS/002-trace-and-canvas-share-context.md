# Keep The Trace Beside The Workflow

Operators should not reconstruct which workflow step produced an event. The canvas selection and run trace remain synchronized, so system behavior and product consequences are inspectable in one place.

The mobile layout preserves this relationship through explicit Canvas, Inspector, and Run views rather than compressing three dense panels into one viewport.
